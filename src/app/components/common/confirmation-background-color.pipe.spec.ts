import { ConfirmationBackgroundColorPipe } from './confirmation-background-color.pipe';

describe('ConfirmationBackgroundColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfirmationBackgroundColorPipe();
    expect(pipe).toBeTruthy();
  });
});
