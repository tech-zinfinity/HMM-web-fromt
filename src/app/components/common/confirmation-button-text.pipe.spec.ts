import { ConfirmationButtonTextPipe } from './confirmation-button-text.pipe';

describe('ConfirmationButtonTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfirmationButtonTextPipe();
    expect(pipe).toBeTruthy();
  });
});
