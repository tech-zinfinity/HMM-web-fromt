import { NotificationModesPipe } from './notification-modes.pipe';

describe('NotificationModesPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationModesPipe();
    expect(pipe).toBeTruthy();
  });
});
